# Baseline data

Run the following command from the repository root to download the public
baseline image data into this directory:

```bash
python download_resources.py --data-only
```

The resulting ImageFolder layout is `Baseline/data/imagenet/{train,val}/...`.
